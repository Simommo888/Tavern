# workers

Background worker boundary for workflow execution, media jobs, streaming jobs and long-running integrations.

Phase 2 does not move worker code yet; existing API worker code remains under `apps/api/app/workers/`.
