# components

Reusable LiveOS component boundary for live-room, studio, analytics and shared composition building blocks.

UI code that is specific to the Next.js app remains under `apps/web/components/` until it becomes reusable.
